apiVersion: v1
kind: Secret
metadata:
  name: gazehum-secrets
type: Opaque
stringData:
  # Replace these values in your cluster or use sealed secrets / a secret store.
  SUPABASE_URL: "REPLACE_ME"
  SUPABASE_KEY: "REPLACE_ME"
